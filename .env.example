# Copy to .env and fill in — never commit .env
# Or set these in ~/.Renviron for R scripts

DB_PASSWORD=
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
AWS_DEFAULT_REGION=us-east-1
R_CONFIG_ACTIVE=default     # set to "production" on EC2
